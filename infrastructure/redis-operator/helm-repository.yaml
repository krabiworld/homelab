apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-operator
spec:
  interval: 1h
  releaseName: redis-operator
  chartRef:
    kind: OCIRepository
    name: redis-operator
