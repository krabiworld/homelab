apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble-http-route
spec:
  parentRefs:
    - name: default
      namespace: default
      sectionName: https
  hostnames:
    - "hubble.home.arpa"
  rules:
    - backendRefs:
        - name: hubble-ui
          port: 80
