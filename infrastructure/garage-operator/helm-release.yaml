apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: garage-operator
spec:
  interval: 1h
  releaseName: garage-operator
  chartRef:
    kind: OCIRepository
    name: garage-operator
